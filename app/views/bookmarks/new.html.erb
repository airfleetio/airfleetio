<%= form_for @bookmark, layout: :basic do |f| %>
  <%= f.text_field :vin, label: 'Enter the VIN of a vehicle to add to your saved list:',
    placeholder: '(e.g.: X0RHJBL60MHDXXWBQ)' %>
  <%= f.submit 'Add vehicle' %>
<% end %>

<hr />

<% if @vehicles.present? %>
<h3>You have <%= pluralize @vehicles.size, 'saved vehicle' %></h3>

<table class="table table-striped">
  <thead>
    <tr>
      <th scope="col">VIN</th>
      <th scope="col">Year</th>
      <th scope="col">Make</th>
      <th scope="col">Model</th>
    </tr>
  </thead>

  <tbody>
    <% @vehicles.each do |vehicle| %>
      <tr>
        <td><%= vehicle.vin %></td>
        <td><%= vehicle.year %></td>
        <td><%= vehicle.make %></td>
        <td><%= vehicle.model %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>
